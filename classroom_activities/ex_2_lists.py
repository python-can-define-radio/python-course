    Try this. Do you expect it to print “water” or “chair”?[Text Wrapping Break]thelist = ["water", "chair", "mug", "mouse"][Text Wrapping Break]print(thelist[1])[Text Wrapping Break] 

    Modify the previous example to print the fourth element of the list (“mouse”). [Text Wrapping Break] 

    Try this. Do you expect an error?[Text Wrapping Break]print(thelist[-1])[Text Wrapping Break] 

    Add a few elements to thelist. Then do this again:[Text Wrapping Break]print(thelist[-1]) 

 

    Now make it print the second-to-last element of thelist.[Text Wrapping Break] 

    Try this. It’s a little counterintuitive.[Text Wrapping Break]print(thelist[0:3])[Text Wrapping Break] 

    Try this.[Text Wrapping Break]print(thelist[1:3]) 

 

    Modify the previous example to print elements 0,1,2,3. (Hint: it’s not going to be [0:3], but it’s close to that.)[Text Wrapping Break] 

    Try this.[Text Wrapping Break]print(thelist[1:])[Text Wrapping Break] 

    Modify the previous example to print all but the first two items. Pretend you don’t know how long the list is. (You shouldn’t need to.)[Text Wrapping Break] 

    Try this.[Text Wrapping Break]letters = [“a”, “b”, “c”, “d”, “e”][Text Wrapping Break]letters[0] = “ROCKET”[Text Wrapping Break] 

    Modify the previous example to replace “b” with “ROCKET” instead of replacing “a”.[Text Wrapping Break] 

    Modify the previous example to replace the second-to-last element with “ROCKET”.[Text Wrapping Break] 

    Try this.[Text Wrapping Break]name = “Becky”[Text Wrapping Break]print(name[0])[Text Wrapping Break] 

    Modify the previous example to print the “c” of “Becky”.[Text Wrapping Break] 

 

 

    Try this.[Text Wrapping Break]names = [“Sarah”, “Tom”, “Harry”, “Susan”, “Becky”][Text Wrapping Break]print(names[4][3])[Text Wrapping Break] 

    Modify the previous example to print the last letter of Sarah’s name (using string indexing).[Text Wrapping Break] 

    Modify the previous example to print the last letter of the last name in the list. Use negative indexing.[Text Wrapping Break] 
